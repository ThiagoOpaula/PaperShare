import styled from 'styled-components/native';
import { SwipeablePanel } from 'rn-swipeable-panel';

export const Container = styled.View`
  width: 100%;
  height: 100%;
`;

export const Panel = styled(SwipeablePanel)`
  background-color: #eee;
`;
